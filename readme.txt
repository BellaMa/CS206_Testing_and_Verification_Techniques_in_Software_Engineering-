In each program’s directory

    first, “python generate_test_suites.py” to generate 9 test suites (output_*.txt) and output.txt

    second, “python cp.py” to copy program’s input directories to each fault version’s folder

    third, “python nextStep.py” to generate results.txt which contains the number of faults exposed and exposed faults’ version
